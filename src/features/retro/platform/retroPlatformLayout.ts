export const RETRO_DETAIL_SCENE_LAYOUT = Object.freeze({
  television: Object.freeze({
    position: Object.freeze([0.15, 0.55, -0.65] as const),
    scale: 1.05,
  }),
  hardware: Object.freeze({
    position: Object.freeze([0.15, -1.2, 0.75] as const),
    scale: 1.15,
  }),
  gameCaseLane: Object.freeze({
    minX: 2,
  }),
});
